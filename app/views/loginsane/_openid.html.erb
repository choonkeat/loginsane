<%= link_to image_tag(service.icon, :class => 'service_icon'), :controller => 'loginsane', :action => 'openid_callback', :id => service.id, :openid_url => (service.key.blank? ? nil : service.key), :only_path => false %>